<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_form_care" model="ir.ui.view">
        <field name="name">product.template.form.care</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="after">
                <page string="Home Care" name="home_care">
                    <group>
                        <group>
                            <field name="is_care_package"/>
                            <field name="is_care_service"/>
                            <field name="request_category_id" invisible="not is_care_service"/>
                            <field name="default_care_team_id" invisible="not is_care_service"/>
                        </group>
                    </group>
                    <group string="Package Services" invisible="not is_care_package">
                        <field name="care_package_line_ids" nolabel="1">
                            <list editable="bottom">
                                <field name="sequence" widget="handle"/>
                                <field name="included_service_id"/>
                                <field name="quantity"/>
                                <field name="category_id"/>
                            </list>
                        </field>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
